const router=require("express").Router();const c=require("../controllers/authController");const {redirectIfAuthenticated}=require("../middleware/authMiddleware");router.get("/login",redirectIfAuthenticated,c.loginPage);router.post("/login",c.login);router.get("/register",redirectIfAuthenticated,c.registerPage);router.post("/register",c.register);router.post("/logout",c.logout);module.exports=router;